eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
server:
  port: 8769
spring:
  application:
    name: service-ribbon
feign:
  hystrix:
    enabled: true